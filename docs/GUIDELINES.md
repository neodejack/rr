# Guidelines

This document is the index for the detailed guideline pages under `docs/guidelines/`. Read the page that matches the change you are making.

## Guideline Map

- `docs/guidelines/development.md`: canonical setup, compile, test, release, and dev-shell commands.
- `docs/guidelines/style.md`: Elixir formatting and naming conventions.
- `docs/guidelines/testing.md`: ExUnit, focused test runs, and the repository's provider mocking pattern.
- `docs/guidelines/collaboration.md`: commit message and pull request conventions.
- `docs/guidelines/security.md`: local state, secrets, and config-handling rules.

## Progressive Disclosure

Use the shallowest page under `docs/guidelines/` that answers your question. Start with the page that best matches the task in front of you, and only read additional pages when you need more detail.
