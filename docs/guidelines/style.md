# Coding Style

Use standard Elixir formatting and naming.

- Run `mix format` or `mix format --check-formatted`; the repo expects 2-space Elixir formatting.
- Keep module and function names clear and lower_snake_case at the function level.
